// Legacy route component retained for compatibility. The normalized team dashboard
// now serves leader and member views from the same canonical finding model.
import TeamDashboardPage from './TeamDashboardPage'

export default function LeaderDashboardPage() {
  return <TeamDashboardPage />
}
